const emptyNode = { value: 0, next: null };

const phasedSum = (firstNode, secondNode, carry) => {
  return 0;
};

const sumList = (firstList, secondList) => {
  console.log("##### firstNode", firstList.length);
  console.log("##### secondNode", secondList.length);

  return [0];
};

export default sumList;
