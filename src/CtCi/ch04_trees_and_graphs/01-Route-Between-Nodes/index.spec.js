import Graph from "../util/Graph.js";

describe("Class 테스트", function () {
  describe("Graph 초기화", function () {
    it("Graph 인스턴스 생성 확인", function () {
      const graph = new Graph();
      console.log(graph);
    });
  });
});
